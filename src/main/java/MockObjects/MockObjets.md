# Mock Objects

A mock objects implements the expected behaviour when interacting
with various types of controllers or sensors.

Each mock object should have 2 things:

* It's current state.
* Methods to output it's state.